"""광고 문구 생성 프롬프트와 이미지 포스터 프롬프트를 만듭니다."""

# 제품 정보를 읽기 쉬운 JSON 문자열로 만들기 위해 불러옵니다.
import json

# 제품 정보와 광고 문구 데이터 형식을 불러옵니다.
from models import AdCopy, ProductBrief


# 포스터 스타일별 구체적인 디자인 지시입니다.
STYLE_GUIDES: dict[str, str] = {
    "미니멀 제품 포스터": (
        "불필요한 장식을 줄이고 제품이 가장 먼저 보이는 현대적인 미니멀 포스터. "
        "정돈된 여백과 명확한 정보 크기 차이를 사용합니다."
    ),
    "프리미엄 브랜드 포스터": (
        "절제된 소품, 세련된 조명과 공간감을 활용한 고급 브랜드 포스터. "
        "제품의 재질과 품질이 돋보이게 구성합니다."
    ),
    "귀엽고 발랄한 포스터": (
        "친근하고 경쾌한 그래픽 요소를 활용한 밝은 제품 포스터. "
        "장식은 제품을 가리지 않고 지나치게 복잡하지 않게 사용합니다."
    ),
    "실사 라이프스타일 포스터": (
        "제품이 실제 생활 공간에서 자연스럽게 사용되는 듯한 실사형 포스터. "
        "주변 소품은 제품의 사용 장면을 돕는 정도로만 배치합니다."
    ),
    "시즌 이벤트 포스터": (
        "계절이나 행사 분위기가 분명히 느껴지는 이벤트형 제품 포스터. "
        "이벤트 요소보다 제품과 핵심 문구가 먼저 보이게 구성합니다."
    ),
    "창의적인 두들 포스터": (
        "비유와 은유를 활용한 자유로운 두들(낙서) 스타일 포스터. "
        "독창적인 재료와 거친 질감을 결합하고, 예술 사조를 현대적으로 재해석하며, "
        "역동적인 빛과 색상의 조화를 통해 창의적인 화면을 구성합니다."
    ),
}


# 포스터 색감별 상세 지시입니다.
COLOR_GUIDES: dict[str, str] = {
    "자동 추천": "제품 색상과 포스터 스타일에 가장 잘 어울리는 조화로운 색감을 선택합니다.",
    "밝은 파스텔": "부드러운 파스텔 색을 사용하되 제품 고유 색상과 충돌하지 않게 합니다.",
    "선명한 비비드": "생동감 있는 선명한 색을 사용하되 제품이 묻히지 않도록 대비를 조절합니다.",
    "차분한 뉴트럴": "화이트, 베이지와 회색 계열의 정돈되고 차분한 색감을 사용합니다.",
    "고급스러운 다크": "짙은 배경과 섬세한 조명으로 고급스러운 분위기를 만듭니다.",
}


# 포스터 배경별 상세 지시입니다.
BACKGROUND_GUIDES: dict[str, str] = {
    "AI 자동 추천": "제품과 선택한 포스터 스타일에 가장 적절한 배경을 자동으로 구성합니다.",
    "깔끔한 스튜디오": "제품 촬영 스튜디오처럼 정돈된 배경과 자연스러운 그림자를 사용합니다.",
    "실사 공간": "제품이 실제로 사용될 법한 현실적인 공간을 구성합니다.",
    "부드러운 그라데이션": "제품과 어울리는 부드러운 그라데이션 배경을 사용합니다.",
    "그래픽 패턴": "제품을 가리지 않는 단순한 그래픽 패턴과 도형을 사용합니다.",
}


# 글자 양에 따른 상세 지시입니다.
TEXT_AMOUNT_GUIDES: dict[str, str] = {
    "적게": "제품명, 제목과 필요한 경우 가격만 사용하고 설명은 매우 짧게 배치합니다.",
    "보통": "제목, 부제목, 짧은 설명과 구매 유도 문구를 균형 있게 사용합니다.",
    "많게": "제목, 부제목, 설명, 가격, 수량과 구매 유도 문구를 모두 정돈해 사용합니다.",
}


# 광고 문구를 만드는 AI가 항상 따라야 할 공통 규칙입니다.
COPY_SYSTEM_PROMPT = """
당신은 한국어 제품광고 포스터를 전문으로 작성하는 카피라이터입니다.

반드시 지켜야 할 규칙:
1. 사용자가 입력한 사실만 사용합니다.
2. 제공되지 않은 효능, 인증, 원산지, 소재, 수치와 구성품을 만들어내지 않습니다.
3. 제품명, 브랜드명, 가격과 수량을 임의로 바꾸지 않습니다.
4. 제목, 부제목, 설명과 구매 유도 문구가 같은 내용을 반복하지 않게 합니다.
5. 포스터에서 읽기 쉽도록 짧고 명확하게 작성합니다.
6. 확인되지 않은 최상급 표현과 허위·과장 표현을 사용하지 않습니다.
7. 이모지와 불필요한 특수문자를 사용하지 않습니다.
8. 지정된 구조화 출력 형식을 정확히 지킵니다.
""".strip()


def build_copy_user_prompt(brief: ProductBrief) -> str:
    """제품 정보를 광고 문구 생성용 프롬프트로 바꿉니다."""

    # 제품 정보를 한글이 유지되는 JSON 문자열로 만듭니다.
    product_json = json.dumps(
        brief.model_dump(mode="json"),
        ensure_ascii=False,
        indent=2,
    )

    # AI가 작성해야 할 항목과 기준을 함께 전달합니다.
    return f"""
아래 제품 정보로 제품광고 포스터에 사용할 문구를 작성하세요.

제품 정보:
{product_json}

출력 기준:
- title: 포스터에서 가장 크게 보이는 짧은 제목
- subtitle: 제목을 보완하되 제목과 같은 말을 반복하지 않는 문구
- description: 입력된 제품 특징만 이용한 자연스럽고 짧은 설명
- cta: 구매, 방문 또는 문의를 유도하는 짧은 문구
- badge: 실제 제품에 적용할 수 있는 짧은 강조 문구
- price_text: 가격 입력값이 있으면 그대로 사용하고 없으면 빈 문자열
- quantity_text: 수량 입력값이 있으면 그대로 사용하고 없으면 빈 문자열
- hashtags: 제품명과 카테고리에 맞는 한글 해시태그 3~5개
- 사용자가 선택한 포스터 글자 양은 '{brief.text_amount}'입니다.
""".strip()


def _build_exact_text_block(brief: ProductBrief, ad_copy: AdCopy) -> str:
    """이미지에 사용할 문구 중 빈 값은 제외하고 정리합니다."""

    # 포스터에 표시할 수 있는 문구를 이름과 값으로 묶습니다.
    text_items = [
        ("브랜드", brief.brand_name),
        ("제품명", brief.product_name),
        ("제목", ad_copy.title),
        ("부제목", ad_copy.subtitle),
        ("설명", ad_copy.description),
        ("가격", ad_copy.price_text),
        ("수량", ad_copy.quantity_text),
        ("구매 유도 문구", ad_copy.cta),
        ("강조 배지", ad_copy.badge),
    ]

    # 값이 비어 있지 않은 항목만 한 줄씩 반환합니다.
    return "\n".join(
        f"{label}: {value}"
        for label, value in text_items
        if value.strip()
    )


def build_image_prompt(brief: ProductBrief, ad_copy: AdCopy) -> str:
    """제품 사진을 참고해 포스터를 만들 이미지 AI 프롬프트를 생성합니다."""

    # 선택한 포스터 스타일 지시를 가져옵니다.
    style_guide = STYLE_GUIDES.get(
        brief.style,
        STYLE_GUIDES["미니멀 제품 포스터"],
    )

    # 선택한 색감 지시를 가져옵니다.
    color_guide = COLOR_GUIDES.get(
        brief.color_tone,
        COLOR_GUIDES["자동 추천"],
    )

    # 선택한 배경 지시를 가져옵니다.
    background_guide = BACKGROUND_GUIDES.get(
        brief.background_type,
        BACKGROUND_GUIDES["AI 자동 추천"],
    )

    # 선택한 글자 양 지시를 가져옵니다.
    text_guide = TEXT_AMOUNT_GUIDES.get(
        brief.text_amount,
        TEXT_AMOUNT_GUIDES["보통"],
    )

    # 포스터에 반영할 정확한 문구를 정리합니다.
    exact_text = _build_exact_text_block(brief, ad_copy)

    # 단순 링크 전달을 넘어, 저작권 보호와 창의성 지침을 결합합니다.
    reference_guide = ""
    if brief.reference_image_link:
        reference_guide = (
            f"\n[참조 디자인 링크 및 창의적 스타일 지침]\n"
            f"- 다음 링크({brief.reference_image_link})의 시각적 요소를 바탕으로 포스터를 구성하세요.\n"
            f"- 단, 저작권 보호를 위해 원본을 그대로 묘사하거나 표절하지 마세요.\n"
            f"- 비유와 은유를 활용하고, 독창적인 재료와 질감을 결합하여 새로운 화면을 구성하세요.\n"
            f"- 기존 예술 사조를 현대적으로 재해석하며, 역동적인 빛과 색상의 조화를 지침으로 삼으세요.\n"
        )

    # 이미지 AI에 전달할 최종 지시문을 만듭니다.
    generated_prompt = f"""
업로드된 제품 사진을 핵심 참고 이미지로 사용하여 한국어 제품광고 포스터 한 장을 제작하세요.
{reference_guide}
[원본 제품 보존]
- 업로드된 제품의 외형, 비율, 색상, 재질, 라벨, 인쇄와 구성품을 최대한 정확하게 유지하세요.
- 제품의 색상과 모양을 다른 제품처럼 변경하지 마세요.
- 제품 개수와 구성품을 임의로 추가하거나 제거하지 마세요.
- 제품 위의 기존 상표와 패키지 정보를 임의로 바꾸지 마세요.
- 장식이나 글자가 제품을 가리지 않게 하세요.
- 제품은 포스터에서 가장 크고 선명한 핵심 요소여야 합니다.

[포스터 스타일]
{style_guide}

[전체 색감]
{color_guide}

[배경]
{background_guide}

[글자 양]
{text_guide}

[광고 정보]
- 제품 카테고리: {brief.category}
- 주요 고객: {brief.target_customer}
- 사용 목적: {brief.usage}
- 최종 출력 규격: {brief.output_type}

[포스터에 사용할 정확한 한국어 문구]
{exact_text}

[레이아웃 원칙]
- 인쇄 가능한 완성형 제품광고 포스터로 디자인하세요.
- 제목과 제품명이 잘 보이도록 정보의 크기 차이를 분명하게 하세요.
- 가격이 입력된 경우 눈에 잘 보이게 강조하세요.
- 같은 설명을 여러 위치에 반복하지 마세요.
- 모든 문구가 이미지 밖으로 나가거나 잘리지 않게 하세요.
- 중요한 문구는 가장자리에서 충분히 떨어진 안전영역 안에 배치하세요.
- 새로운 상품 정보, 효능, 인증 또는 수치를 추가하지 마세요.
- 한국어 철자를 가능한 정확하게 표현하세요.

[추가 요청]
{brief.extra_request if brief.extra_request else "추가 요청 없음"}
""".strip()

    # 이후 JSON 직렬화 시 실제 이미지 생성 프롬프트도 함께 내보냅니다.
    ad_copy.generated_prompt = generated_prompt

    # 이미지 생성 API에 전달할 최종 프롬프트를 반환합니다.
    return generated_prompt
